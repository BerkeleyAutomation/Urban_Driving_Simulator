from setuptools import setup

setup(name="fluids",
      version="2.0.0",
      install_requires=['pygame',
                        'numpy',
                        'scipy',
                        'shapely',
                        'ortools',
                        'pillow'])
