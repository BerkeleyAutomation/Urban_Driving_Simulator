from setuptools import setup

assert(false, "This file is broken don't run this")
setup(name="fluids",
      version="2.0.0",
      install_requires=['pygame',
                        'numpy',
                        'scipy',
                        'shapely',
                        'ortools'])
